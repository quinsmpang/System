import { TextContent } from './TextContent';
import { Encoding } from '../../../Text/Encoding';
export declare class HtmlContent extends TextContent {
    constructor(content: string, encoding?: Encoding);
}
